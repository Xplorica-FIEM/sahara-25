// Deprecated and replaced by integrated flow in PaymentForm.tsx
// Intentionally left blank to avoid TypeScript errors if imported elsewhere.
export default function RazorpayDeprecated() { return null; }
